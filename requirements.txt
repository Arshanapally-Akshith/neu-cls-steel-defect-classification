# Phase 1 (data prep & integrity) dependencies.
numpy==2.3.5
pandas==2.3.3
pillow==12.2.0
scikit-learn==1.9.0
PyYAML==6.0.3
pytest==9.1.1

# Phase 2 (classical HOG + Logistic Regression baseline).
scikit-image==0.26.0
joblib==1.5.3
matplotlib==3.10.8

# Phase 3 (ResNet18 transfer learning). CPU-only build — install with:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Later phases will add grad-cam, streamlit, etc.
torch==2.13.0
torchvision==0.28.0
